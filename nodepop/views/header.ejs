<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= appName %></title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
        <link rel="stylesheet" href="../stylesheets/common.css">
        <link rel="stylesheet" href="../stylesheets/components.css">
        <link rel="stylesheet" href="../stylesheets/header.css">
        <link rel="stylesheet" href="../stylesheets/products.css">
    </head>
    <body>
      <h1 class="screen-reader-only"><%= appName %></h1>
      <header class="header">
        <nav class="navbar navbar-expand-lg">
            <div class="container container-fluid">
              <div class="navbar-brand">
                <a href="/">
                  <img src="../images/nodepop-logo.svg">
                </a>
              </div>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <% if (session.userId) { %>
                    <li class="nav-item">
                      <a class="btn btn-outline-success" href="/products/new">Add new product</a>
                    </li>
                  <% } %> 
                </ul>
                <% if (!session.userId) { %>
                  <a class="btn btn-outline-success" href="/login">Log In</a>
                <% } else { %>
                  <a class="btn btn-outline-success" href="/logout">Log Out</a>
                <% } %> 
              </div>
            </div>
          </nav>
      </header>

    
